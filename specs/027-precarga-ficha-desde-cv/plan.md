# Plan — SPEC-027

Persistir extracción en `ResumeDocument`, exponer sugerencias por API y aplicar solo campos permitidos con bloqueo optimista. Reutilizar el grafo existente; si no hay LLM, ofrecer contacto determinista cuando sea verificable. Rollback oculta el panel y mantiene CV/evaluación. Probar aceptación selectiva, corrección, ausencia, permisos y auditoría.
